You are a code generation agent working on a Python refactor.

Context:

* There is a `TranslationRunner` class in `Translator.py`.
* Current problem: `TranslationRunner` is tightly coupled to console I/O (prints, “press enter to continue”, error prompts, retry via `input()`, etc.).
* Because of that, `GUI.py` currently avoids using `TranslationRunner` and is effectively decoupled from it. This is undesirable.

Goal:
Decouple `TranslationRunner` from any concrete I/O so that both `CLI.py` and `GUI.py` can use the same runner by providing appropriate I/O implementations.

Requirements:

1. Introduce abstraction(s) for I/O

* Define one or more protocols (use `typing.Protocol`) that represent the I/O capabilities `TranslationRunner` needs.
* These protocols must cover:

  * emitting normal output (status/progress/info)
  * emitting errors
  * requesting user input (including “continue”, “retry”, etc.) in a UI-agnostic way. Se
  * optionally: progress updates / cancellation checks, if the existing code implies it
  * `TranslationRunner` must depend only on these protocols, not on `print()` or `input()` or any GUI toolkit. 
  * refactoring policy.py to reflect the former

2. Refactor `TranslationRunner`

* Remove all direct console interactions from `TranslationRunner`.
* Replace them with calls to the injected protocol(s).
* Keep `TranslationRunner` responsible for orchestration and translation flow, not presentation.
* Ensure behavior remains functionally equivalent to current CLI behavior (same decisions, retries, failure handling), but mediated through the protocol.

3. Implement CLI wiring in `CLI.py`

* Provide a concrete console implementation of the protocol(s) (e.g., `ConsoleIO`) using `print()` and `input()`.
* Update `CLI.py` to instantiate `TranslationRunner` with this console implementation.
* It is probably desirable to remove policy.py and add the funcionality to the cli input/output abstracion. 

4. Implement GUI wiring in `GUI.py`

* Update `GUI.py` to use `TranslationRunner` instead of bypassing it.
* Provide a GUI-compatible implementation of the protocol(s) (e.g., `GuiIO`) that routes output/errors to the GUI and obtains user responses via GUI controls/dialogs.
* If GUI interactions are asynchronous, provide an approach compatible with the existing GUI architecture (e.g., callbacks, async methods, queue/event loop integration). Prefer minimal disruption.
* It is probably desirable to remove policy.py and add the funcionality to the GUI input/output abstracion. 

5. Architectural constraints

* Do not introduce hard dependencies between core translation logic and GUI framework code.
* Keep protocols and core logic in a place importable by both CLI and GUI without circular imports.
* Preserve existing public APIs where reasonable; if you change signatures, update call sites accordingly.
* Add type hints throughout.
* Keep changes focused and readable; no unnecessary redesign.

Deliverables:

* Updated `Translator.py` with refactored `TranslationRunner` and protocol definitions (or protocols placed in an appropriate shared module).
* Updated `CLI.py` that uses `TranslationRunner` with a console I/O implementation.
* Updated `GUI.py` that uses `TranslationRunner` with a GUI I/O implementation.
* Any small supporting files/classes needed for clean separation.
* It is probably desirable to remove policy.py. 

Output format:

* Provide a concise explanation of the refactor approach (a few paragraphs).
* Then provide the full updated code for each modified file, clearly separated by filename headers.
